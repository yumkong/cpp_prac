//at most 6 kinds of items,100 special offers
//for each item, need to buy at most 6 of them
// not allowed to buy more items than you want, even if that lowers the price
#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    int shoppingOffers(vector<int>& price, vector<vector<int>>& special, vector<int>& needs) {
        
    }
};